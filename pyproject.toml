[project]
name = "fr3oon-helper-bot"
version = "1.0.0"
description = "Telegram moderation bot for MOBA SHOP card exchange group"
requires-python = ">=3.12"
dependencies = [
    "requests==2.32.3"
]

[tool.vercel]
entrypoint = "api.index:handler"
